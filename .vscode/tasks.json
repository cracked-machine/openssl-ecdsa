{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": [
                "cd cpp &&",
                "rm -rf build &&",
                "cmake -S . -B build &&",
                "cmake --build build"
            ],
            "problemMatcher": []
        }
    ]
}